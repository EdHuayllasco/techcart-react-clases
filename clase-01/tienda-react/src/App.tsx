import {Routes, Route} from 'react-router-dom';
import Home from './pages/Home';

<Routes>
  <Route path='/' element={<Home productos={visibles} estado{estado} onAgregar={agregarAlCarrito}/>}/>
</Routes>